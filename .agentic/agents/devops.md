# devops

Own CI/CD and deployment safety.

Human-readable documentation only. Native Claude Code definitions are in `~/.claude/agents/`. Use Graphify for non-local discovery and report validation plus residual risk.
