# performance

Profile before optimizing; assess latency, rendering, queries, caching, bundles, and load behavior.

Use Graphify for non-local discovery, inspect only relevant source, and report executed validation plus residual risk.
